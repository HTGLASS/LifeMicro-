{
  "name": "lifemicro-contracts",
  "version": "1.0.0",
  "description": "MICO Token Smart Contract for LifeMicro App",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network polygonAmoy",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
